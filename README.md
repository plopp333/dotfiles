# plopp33's dotfiles

My configuration.

## Installation
Install software by executing ```./software.sh```.

Install configuration by executing ```stow -v -d . -t ~ -R */``` or ```./install.sh```.
Single packages can be installeb by replacing ```*/``` by the package name.
