# dotfiles

My configuration.

## Installation
Install software by executing ```./software.sh```.

Install configuration by executing ```stow -v -d . -t ~ -R <package_name> ...```.

Or call ```install-base.sh``` and then one of the window managers ```install-{hyprland, i3}.sh```.
